import React from 'react';

const DiaryPaperItem = ({
  // commentCount,
  // condition,
  // content,
  // createdAt,
  // date,
  // diaryId,
  // images,
  // likeCount,
  // myHeart,
  // name,
  // open,
  // paperId,
  // symptomList,
  // title,
  // uid,
  diaryId,
  dayClicked,
}) => {
  return (
    <>
      <span>{diaryId}</span>
      {/* <span>{dayClicked}</span> */}
    </>
  );
};

export default DiaryPaperItem;
