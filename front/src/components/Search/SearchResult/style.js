import styled from 'styled-components';
import { BoxGrad } from '../../common/BoxGrad/BoxGrad';

const BoxSearchResult = styled(BoxGrad)`
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 1em 5.5em;
  border-radius: 0;
`;

export { BoxSearchResult };
