import styled from 'styled-components';

const BoxBtnToTop = styled.div`
  text-align: center;
  padding: 1.5em 0 1em;
  color: var(--icon-color);
  span {
    cursor: pointer;
  }
`;

export { BoxBtnToTop };
