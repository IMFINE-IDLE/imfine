package com.idle.imfine.service.suggestion;

import com.idle.imfine.data.dto.suggestion.request.RequestSuggestionDto;

public interface SuggestionService {
    void save(RequestSuggestionDto requestSuggestionDto);
}
